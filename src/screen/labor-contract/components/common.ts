import { styled, Typography } from '@mui/material'

export const Heading = styled(Typography)({
    fontWeight: 'bold',
    fontSize: '1rem',
    marginBottom: '0.75rem',
    marginTop: '1rem',
    color: '#000'
})

export const grid_input = {
    xs: 12,
    sm: 3
}

export const grid_company_settings = {
    xs: 12,
    sm: 4
}

export const grid_full = {
    xs: 12
}

export const grid_half = {
    xs: 12,
    // sm: 10,
    md: 12,
    lg: 12,
    xl: 8
}

export const grid_half_v2 = {
    xs: 12,
    sm: 6,
    // md: 6,
    // lg: 12,
    xl: 6
}
